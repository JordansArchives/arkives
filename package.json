{
  "name": "arkives",
  "version": "0.1.0",
  "private": true,
  "description": "Arkives — creator partnership CRM (vanilla JS as native ES modules + Supabase, served from public/ on Cloudflare Workers, no bundler)",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "npm run test:checks && npm run test:smoke",
    "test:checks": "node tests/checks.mjs",
    "test:smoke": "node tests/smoke.mjs",
    "serve": "node --input-type=module -e \"import('./tests/_server.mjs').then(m => m.startServer(8741)).then(() => console.log('http://localhost:8741 (serving public/ with the production headers)'))\"",
    "test:live": "node tests/live.mjs"
  },
  "devDependencies": {
    "eslint": "^9.39.0",
    "globals": "^16.0.0",
    "playwright-core": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
